import { UserRound, RotateCcw, Home } from 'lucide-react';
import SceneLayout from '../components/SceneLayout';
import Diamond from '../components/Diamond';
import GameButton from '../components/GameButton';
import CharacterPortrait from '../components/CharacterPortrait';
import { team } from '../data/storyData';
export function TeamScene(){return <SceneLayout eyebrow="THE PEOPLE BEHIND THE SIMULATION" title="DEVELOPMENT TEAM" className="team-scene"><div className="team-roster">{team.map((member,i)=><article className={`team-member role-${member.roleTone}`} key={member.studentId}><div className="team-avatar">{member.portraitId?<CharacterPortrait id={member.portraitId} float={false}/>:<UserRound strokeWidth={1.2}/>}<span>{String(i+1).padStart(2,'0')}</span></div><div className="team-member-card"><div className="team-role-badge">{member.role}</div><h2>{member.name}</h2><p className="team-chinese">{member.chineseName}</p><div className="team-student-id"><span>STUDENT ID</span><strong>{member.studentId}</strong></div></div></article>)}</div><p className="team-signoff"><Diamond/> GROUP 1 <span>THANKS FOR PLAYING</span><Diamond/></p></SceneLayout>;}
export function EndScene({dispatch}){return <SceneLayout className="end-scene"><div className="end-orbit"/><Diamond/><span className="eyebrow">GROUP 1</span><h1>CASE COMPLETE?<span>.</span></h1><div className="end-copy"><p>No perfect ending.<br/>Only choices, trade-offs, and consequences.</p><p>N/A is not a score.</p><p>Data doesn’t make decisions. People do.</p></div><div className="end-actions"><GameButton variant="white" onClick={()=>dispatch({type:'HOME'})}><Home/>HOME</GameButton><GameButton variant="green" onClick={()=>dispatch({type:'RESTART'})}><RotateCcw/>RESTART SIMULATION</GameButton></div></SceneLayout>;}
