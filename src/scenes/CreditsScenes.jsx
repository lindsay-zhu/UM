import { UserRound, RotateCcw, Home } from 'lucide-react';
import SceneLayout from '../components/SceneLayout';
import Diamond from '../components/Diamond';
import GameButton from '../components/GameButton';
import { team } from '../data/storyData';
export function TeamScene(){return <SceneLayout eyebrow="THE PEOPLE BEHIND THE SIMULATION" title="DEVELOPMENT TEAM" className="team-scene"><div className="team-roster">{team.map((member,i)=><div className="team-member" key={i}><div className="team-avatar">{member.portrait?<img src={member.portrait} alt={member.name||`Team member ${i+1}`}/>:<UserRound strokeWidth={1.2}/>}<span>{String(i+1).padStart(2,'0')}</span></div><h2>{member.name||`Member ${i+1}`}</h2><p>{member.role}</p></div>)}</div><p className="team-signoff"><Diamond/> GROUP 1 <span>THANKS FOR PLAYING</span><Diamond/></p></SceneLayout>;}
export function LaborScene(){return <SceneLayout eyebrow="GROUP 1" title="DIVISION OF LABOR" className="labor-scene"><div className="panel labor-panel"><table><thead><tr><th>Member</th><th>Contribution</th></tr></thead><tbody>{team.map((member,i)=><tr key={i}><td><span className="member-index">0{i+1}</span>{member.name||`Member ${i+1}`}</td><td>{member.contribution||<span className="unassigned">To be added</span>}</td></tr>)}</tbody></table></div></SceneLayout>;}
export function EndScene({dispatch}){return <SceneLayout className="end-scene"><div className="end-orbit"/><Diamond/><span className="eyebrow">GROUP 1</span><h1>THANK YOU<span>.</span></h1><p>THANKS FOR PLAYING</p><div className="end-actions"><GameButton variant="white" onClick={()=>dispatch({type:'HOME'})}><Home/>HOME</GameButton><GameButton variant="green" onClick={()=>dispatch({type:'RESTART'})}><RotateCcw/>RESTART SIMULATION</GameButton></div></SceneLayout>;}
